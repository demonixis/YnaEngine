using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace Yna.Input.Service
{
    public interface ITouchService
    {/*
        bool IsMoved(int point);
        bool IsPressed(int point);
        bool IsReleased(int point);
        bool IsInvalid(int point);
        int GetPoints();
        int CountPoints();
        float GetPressureLevel(int point);
        int GetX(int point);
        int GetY(int point);
        int GetFirstX();
        int GetFirstY();
        float GetFirstPressureLevel();*/
    }
}
